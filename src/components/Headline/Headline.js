import "./Headline.css";
import Card from "../UI/Card";

const Headline = () => {
  return (
    <Card className="headline">
        <h1>Expense Tracker App</h1>
    </Card>
  );
};

export default Headline;
